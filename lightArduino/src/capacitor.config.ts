import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.claudio.lightArduino',
  appName: 'lightArduino',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
