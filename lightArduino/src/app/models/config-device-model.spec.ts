import { ConfigDeviceModel } from './config-device-model';

describe('ConfigDeviceModel', () => {
  it('should create an instance', () => {
    expect(new ConfigDeviceModel()).toBeTruthy();
  });
});
