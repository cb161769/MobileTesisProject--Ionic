import { ConnectionConsumptions } from './connection-consumptions';

describe('ConnectionConsumptions', () => {
  it('should create an instance', () => {
    expect(new ConnectionConsumptions()).toBeTruthy();
  });
});
