import { ConnectionRealtimeData } from './connection-realtime-data';

describe('ConnectionRealtimeData', () => {
  it('should create an instance', () => {
    expect(new ConnectionRealtimeData()).toBeTruthy();
  });
});
