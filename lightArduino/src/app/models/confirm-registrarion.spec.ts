import { ConfirmRegistrarion } from './confirm-registrarion';

describe('ConfirmRegistrarion', () => {
  it('should create an instance', () => {
    expect(new ConfirmRegistrarion()).toBeTruthy();
  });
});
