import { ConnectionsRealtimeDataModel } from './connections-realtime-data-model';

describe('ConnectionsRealtimeDataModel', () => {
  it('should create an instance', () => {
    expect(new ConnectionsRealtimeDataModel()).toBeTruthy();
  });
});
