import { ConnectionsSchedule } from './connections-schedule';

describe('ConnectionsSchedule', () => {
  it('should create an instance', () => {
    expect(new ConnectionsSchedule()).toBeTruthy();
  });
});
